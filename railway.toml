[build]
builder = "nixpacks"
buildCommand = "pip install -r backend/requirements.txt"
nixpacksConfigPath = "backend"

[deploy]
startCommand = "cd backend && python app.py"
healthcheckPath = "/api/health"
restartPolicyType = "ON_FAILURE"

[service]
name = "smart-mfg-survey"
